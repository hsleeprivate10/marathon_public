{
  "name": "marathon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Korean marathon race data collection pipeline — fixture-first source adapters to canonical races.json",
  "scripts": {
    "collect": "bun run src/collect.ts",
    "validate": "bun run src/validate.ts",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "zod": "^3.24.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "ky": "^2.0.2"
  }
}
